<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.FeedBackMapper">

	<select id="selectAllFeedBack" resultType="com.entity.FeedBack">
			select * from feedback
	</select>
	
	<insert id="insertFeedBack" parameterType="com.entity.FeedBack" >
	  insert into feedback (time, userId, user, content)
	  values (
	  #{time,jdbcType=TIMESTAMP}, 
	  #{userId,jdbcType=VARCHAR}, 
	  #{user,jdbcType=VARCHAR}, 
	  #{content,jdbcType=VARCHAR})
	</insert>
  
<!-- 
	<update id="updateFeedBack" parameterType="com.entity.FeedBack" >
	  update feedback
	  set fid = #{aname,jdbcType=VARCHAR},
	    time = #{apassword,jdbcType=VARCHAR},
	    userId = #{atype,jdbcType=VARCHAR},
	    user = #{atype,jdbcType=VARCHAR},
	    content = #{atype,jdbcType=VARCHAR},
	    isReplied = #{atype,jdbcType=VARCHAR},
	    repliedMsg = #{atype,jdbcType=VARCHAR},
	    repliedTime = #{atype,jdbcType=VARCHAR}
	  where fid = #{aid,jdbcType=INTEGER}
	</update>
 -->	
	
</mapper> 